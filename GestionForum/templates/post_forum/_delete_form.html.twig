<form method="post" action="{{ path('app_post_forum_delete', {'id': post_forum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_forum.id) }}">
    <div class="row mb-3">
        <button class="btn btn-danger">Delete</button>
    </div>
</form>
